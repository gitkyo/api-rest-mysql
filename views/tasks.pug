Doctype 
    html
        head  
            title Task Manager    
            link(rel='stylesheet', href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css')        
        body
            //add nav bootstrap
            nav.navbar.navbar-expand-lg.navbar-light.bg-light
                a.navbar-brand(href='/') Task Manager
                button.navbar-toggler(type='button', data-bs-toggle='collapse', data-bs-target='#navbarNav', aria-controls='navbarNav', aria-expanded='false', aria-label='Toggle navigation')
                    span.navbar-toggler-icon
                div.collapse.navbar-collapse#navbarNav
                    ul.navbar-nav
                        li.nav-item
                            a.nav-link(aria-current='page', href='/') Home
                        li.nav-item
                            a.nav-link.active(aria-current='page', href='/tasks') Tasks
            h1#myHeading List of Tasks            
            //list of tasks via a loop on data
            ul#myList
                each task in tasks
                    li=task.description
            // form to add a new task with post methgod on tasks route
            form#myForm(role='form' action='/tasks' method='POST')
                div.form-group
                    label(for='addTasks') Ajouter une tâche :
                    input#addTasks.form-control(type='text', value="", placeholder='Add a new task', name='description' )                
                button.btn.btn-primary(type="submit") Submit 

            //if status is not empty, display it
            if status
                p #{status}
            
          
            

